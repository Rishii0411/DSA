2025-12-12 | Word Search in a Matrix

	Question
Given a matrix mat[][] and a string word,  
check whether the word exists in the matrix by moving to adjacent (up, down, left, right) cells without reusing any cell.

	Approach
1.	This is a classic **backtracking / DFS** problem.
2.	Iterate over every cell in the matrix:
	-	If mat[i][j] matches word[0], try to build the word starting from this cell.
3.	Use **Depth-First Search (DFS)**:
	-	At each step, match the current character.
	-	Mark the cell as visited to avoid reuse.
	-	Explore all four possible directions:
		up, down, left, right.
4.	If all characters of the word are matched:
	-	Return true.
5.	If a path fails:
	-	Backtrack by unmarking the cell.
6.	If no starting position leads to a valid path:
	-	Return false.
7.	Time Complexity:
	-	O(n × m × 4^L), where L is the length of the word.
8.	Space Complexity:
	-	O(L) recursion stack.

	Solution

class Solution {
public:
    bool dfs(int i, int j, int idx, vector<vector<char>>& mat, string& word) {
        if (idx == word.size())
            return true;

        int n = mat.size();
        int m = mat[0].size();

        if (i < 0 || j < 0 || i >= n || j >= m || mat[i][j] != word[idx])
            return false;

        char temp = mat[i][j];
        mat[i][j] = '#';  // mark visited

        bool found =
            dfs(i + 1, j, idx + 1, mat, word) ||
            dfs(i - 1, j, idx + 1, mat, word) ||
            dfs(i, j + 1, idx + 1, mat, word) ||
            dfs(i, j - 1, idx + 1, mat, word);

        mat[i][j] = temp; // backtrack
        return found;
    }

    bool exist(vector<vector<char>>& mat, string word) {
        int n = mat.size();
        int m = mat[0].size();

        for (int i = 0; i < n; i++) {
            for (int j = 0; j < m; j++) {
                if (dfs(i, j, 0, mat, word))
                    return true;
            }
        }
        return false;
    }
};
